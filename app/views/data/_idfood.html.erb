

	<h2>Suggestion Indonesian top dishes</h2>
	<ul>
		
    		<% TopFood.find_each(start: 20, batch_size: 20) do |foods| %> 
			<% rand_number = rand(10) + 1 %>   
    			<% @foods = TopFood.find(rand_number) %>
    				<li><%= @foods.name  %></li> 
    			<% puts @foods.inspect %> 
		<% end %>

	</ul>
