

	<h2>Suggestion Indonesian top dishes</h2>
	<ul>
		
    		<% TopFood.where(:country => "ind").find_each do  %> 
			<% rand_number = rand(52) + 1 %> 
    			<% @foods = TopFood.find(rand_number)%>
    				<li><%= @foods.name  %></li> 
    			<% puts @foods.inspect %> 
		<% end %>

	</ul>
